#!/usr/bin/env bash
set -euo pipefail
# Supervisor action: freeze the reviewed TODO before giving it to a worker.
. "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_pybin.sh"
PY="$(yeoul_pybin)" || yeoul_pybin_die
exec "$PY" "$(dirname "${BASH_SOURCE[0]}")/verify_core.py" baseline "$@"
