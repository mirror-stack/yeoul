{
  "_comment": "Legacy trusted-local example only, not managed mode. Recommended: yeoul setup, then yeoul connect --workspace FOLDER to generate configuration. Mirror is independent and optional; configure it separately if needed.",
  "mcpServers": {
    "yeoul": {
      "command": "yeoul-mcp"
    }
  }
}
