{
  "_comment": "Register BOTH servers together. Yeoul (practice layer) composes with mirror-stack (discipline primitive: pre-registration + tamper-evident ledger). Merge this into your MCP client config (Claude Desktop/Code: mcpServers). Without mirror-stack, Yeoul still runs — sealing degrades to a no-op.",
  "mcpServers": {
    "mirror-stack": {
      "command": "mirror-stack-mcp"
    },
    "yeoul": {
      "command": "yeoul-mcp"
    }
  }
}
